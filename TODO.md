# TODO

## 🖥️ CLI-интерфейс в `__main__.py`

Заменить текущий минималистичный вызов на полноценный CLI с аргументами:

- `--use-engines` — список движков через запятую (какие использовать для поиска)
- `--log-level` — уровень логирования (`DEBUG`, `INFO`, `WARNING`, `ERROR`)
- `--artist`, `--title`, `--download-url`, `--meta`, `--engine` — фильтрация выводимых полей трека
- `--json` — вывод результатов в формате JSON (для парсинга в скриптах)
- Другие полезные флаги по мере необходимости

## ⚙️ Система конфигураций

Реализовать двухуровневую конфигурацию:

- **Глобальная** — общие настройки для всех движков (таймауты, лимиты, путь к кэшу и т.д.)
- **Специфичная** — настройки конкретного движка (API-ключи, базовые URL, особенности парсинга)
- **Контекст** — возможность временно переопределить глобальную конфигурацию (например, через `with config.override(...)`), не меняя её навсегда

## 🔍 Фильтрация движков в `msoc.search()`

Дать возможность вызывать `msoc.search(query, engines=["engine1", "engine3"])`, чтобы использовать только указанные движки, игнорируя остальные.
